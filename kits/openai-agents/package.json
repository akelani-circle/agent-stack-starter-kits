{
  "name": "@agent-stack-starter-kits/kit-openai-agents",
  "version": "0.0.0",
  "private": true,
  "description": "Autonomous Payment Agent example using the OpenAI Agents SDK + Circle Agent Stack.",
  "type": "module",
  "engines": {
    "bun": ">=1.2",
    "node": ">=22.15.0"
  },
  "scripts": {
    "demo": "bun run src/index.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@agent-stack-starter-kits/agent-cli": "workspace:*",
    "@agent-stack-starter-kits/circle-tools": "workspace:*",
    "@agent-stack-starter-kits/kit-core": "workspace:*",
    "@openai/agents": "^0.11.0",
    "dotenv": "^17.4.2",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
