{
  "name": "@agent-stack-starter-kits/kit-core",
  "version": "0.0.0",
  "private": true,
  "description": "Framework-agnostic building blocks shared across the Circle Agent Stack kits: marketplace skill fetching, single-sourced tool descriptions, payment preflight helpers, and a terminal theme.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./balance": "./src/balance.ts",
    "./skill": "./src/skill.ts",
    "./theme": "./src/theme.ts",
    "./tools": "./src/tools.ts"
  },
  "engines": {
    "bun": ">=1.2",
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@agent-stack-starter-kits/circle-tools": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
